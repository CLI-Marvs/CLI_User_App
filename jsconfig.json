{
    "compilerOptions": {
        "baseUrl": ".", // Sets the base directory for your paths
        "paths": {
            "@/*": ["frontend/*"] // Matches your alias in `vite.config.js`
        }
    },
    "include": ["frontend/**/*", "vite.config.js"] // Include your codebase
}
